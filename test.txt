main;
	data:
		integer: a, b, c;

	algorithm:
		a := 0;
		b := 1;
		c := 2;

		counting a downward c * 4 to b - 5;
			if a % 2 = 0;
				print "a is ", a, !; # This is a comment
			else;
				print "a is odd, that's gross", !;
			end if;
		end counting;

		print "Done", !;

end main;
